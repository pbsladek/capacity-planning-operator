apiVersion: v1
kind: Secret
metadata:
  name: capacityplan-fastapi-auth
  # Must match the operator namespace where manager runs.
  namespace: k8s-operator-system
type: Opaque
stringData:
  # Replace with your real token before apply.
  token: "REPLACE_ME"
