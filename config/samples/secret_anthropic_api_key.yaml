apiVersion: v1
kind: Secret
metadata:
  name: capacityplan-anthropic-api
  # Must match the operator namespace where manager runs.
  namespace: k8s-operator-system
type: Opaque
stringData:
  # Replace with your real key before apply.
  apiKey: "sk-ant-REPLACE_ME"
