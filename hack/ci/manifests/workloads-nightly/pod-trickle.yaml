apiVersion: v1
kind: Pod
metadata:
  name: cpo-ci-trickle
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: writer
      image: busybox:1.36
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -eu
          # Nightly trickle pattern: slower sustained writes over ~68m (~1640Mi total, ~80% of 2Gi).
          for i in $(seq 1 410); do
            dd if=/dev/zero of="/data/trickle-${i}.bin" bs=1M count=4 >/dev/null 2>&1
            sync
            sleep 10
          done
          sleep 1800
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: cpo-ci-trickle-pvc
