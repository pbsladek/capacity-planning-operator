apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpo-ci-delayed-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ci-hostpath
  volumeName: cpo-ci-delayed-pv
  resources:
    requests:
      storage: 500Mi
