apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpo-ci-trickle-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cpo-ci-local-path
  resources:
    requests:
      storage: 500Mi
