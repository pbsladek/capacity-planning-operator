apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpo-ci-trickle-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ci-hostpath
  volumeName: cpo-ci-trickle-pv
  resources:
    requests:
      storage: 500Mi
