apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpo-ci-churn-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ci-hostpath
  volumeName: cpo-ci-churn-pv
  resources:
    requests:
      storage: 500Mi
