apiVersion: v1
kind: Pod
metadata:
  name: cpo-ci-trickle
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: writer
      image: busybox:1.36
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -eu
          # Slow trickle growth: 6Mi every 10s for ~6m (~192Mi total).
          for i in $(seq 1 32); do
            dd if=/dev/zero of="/data/trickle-${i}.bin" bs=1M count=6 >/dev/null 2>&1
            sync
            sleep 10
          done
          sleep 1800
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: cpo-ci-trickle-pvc
