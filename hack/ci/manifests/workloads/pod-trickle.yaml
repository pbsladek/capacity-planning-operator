apiVersion: v1
kind: Pod
metadata:
  name: cpo-ci-trickle
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: writer
      image: busybox:1.36
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -eu
          # Slow trickle growth: 3Mi every 10s for ~10m40s (~192Mi total).
          for i in $(seq 1 64); do
            dd if=/dev/zero of="/data/trickle-${i}.bin" bs=1M count=3 >/dev/null 2>&1
            sync
            sleep 10
          done
          sleep 1800
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: cpo-ci-trickle-pvc
