apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpo-ci-steady-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ci-hostpath
  volumeName: cpo-ci-steady-pv
  resources:
    requests:
      storage: 500Mi
