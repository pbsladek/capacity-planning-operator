apiVersion: v1
kind: Service
metadata:
  name: alert-receiver
  namespace: monitoring
  labels:
    app: alert-receiver
spec:
  selector:
    app: alert-receiver
  ports:
    - name: http
      port: 8080
      targetPort: 8080
